services:
  - type: web
    name: direct-rdp-browser
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /health
    autoDeploy: true

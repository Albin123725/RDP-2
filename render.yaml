services:
  - type: web
    name: ultra-minimal-rdp-browser
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /health
    autoDeploy: true

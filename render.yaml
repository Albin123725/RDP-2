services:
  - type: web
    name: vnc-desktop
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    healthCheckPath: /vnc_lite.html
    envVars:
      - key: VNC_PASSWD
        value: "albin4242"
      - key: VNC_RESOLUTION
        value: "1024x576"
      - key: VNC_DEPTH
        value: "16"
    resources:
      memoryMB: 512

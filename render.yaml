# render.yaml for web terminal
services:
  - type: web
    name: my-terminal
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PASSWORD
        generateValue: true
